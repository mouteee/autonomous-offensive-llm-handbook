[pytest]
; The reference suite lives in tests/. The starter exercises under starter/
; are learner-owned and deliberately fail on a fresh clone, so they are run
; explicitly (python3 -m pytest starter/lessonNN_test.py) rather than
; collected with the reference suite.
testpaths = tests
